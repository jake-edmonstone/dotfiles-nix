#!/usr/bin/env bash
set -euo pipefail
target="${1:?Usage: tmux-rename-session.sh <session>}"
printf 'Renaming "%s"\nNew name: ' "$target"
read -r name
[ -n "$name" ] && tmux rename-session -t "$target" "$name"
clear
